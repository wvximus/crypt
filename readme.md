# required library

you will need pyAesCrypt library to run the script
```
pip install pyAesCrypt
```

# run the script

```
python crypt.py
```

choose N_crypt or D_crypt (n or d)
enter filename
enter password

the script will automatically remove the old file